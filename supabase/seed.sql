-- Supabase local seed data
-- Run: npx supabase db reset  (drops all local data and re-applies migrations + this seed)
--
-- Phase 1: placeholder only.
-- Phase 3 (Data Layer & Security) will add dev fixtures for palaces, rooms, and nodes.

-- Example: insert a test user (Supabase Auth handles user creation via the API,
-- but you can pre-seed auth.users for integration tests here in later phases.)

