#!/usr/bin/env sh
set -eu

echo "Running pre-push checks..."
pnpm check:guardrails
pnpm check:vercel-config
pnpm turbo lint
pnpm format:check
pnpm turbo build --filter=@memory-palace/web

echo "Pre-push checks passed."

